# 已停止维护的 macOS 打包流程

macOS 打包不再属于普通构建或发行验收范围，`make:mac`、DMG 和 macOS ZIP 目标已经移除。本文保留旧路径，仅用于避免已有文档链接失效；不要按历史命令生成发行候选。

当前维护流程见 [Store MSIX 打包验收](06-store-msix-packaging-qa.md)。历史 macOS 产物与对应版本说明仍保留在旧 release notes 中，不代表当前发行矩阵。
